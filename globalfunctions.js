function fadeIn(){
    let xPos                    =   window.innerWidth / 2;
    let yPos                    =   window.innerWidth * canRatio / 2; 
    let rectWidth               =   window.innerWidth;
    let rectHeight              =   window.innerWidth * canRatio;
    rectMode(CENTER,CENTER);
    fadeInCounter++;
    rect(xPos, yPos, rectWidth, )

}